{% extends "MHStoreBundle::layout.html.twig" %}


{% block bundle_body %}

    <h3> Suppression d'un produit </h3>

    <hr>



    <div class="card">
        <div class="card-body">
            <div class="row">
                {% include('MHStoreBundle:Template:list-annonce.html.twig') %}
                <div class="col-md-6">
                    <a href="{{ path('mh_store_view', {'id': product.id}) }}" class="btn btn-default">
                        <i class="fa fa-chevron-left" aria-hidden="true"></i>
                        Retour au produit
                    </a>
                </div>
                <div class="col-md-6">
                    <form action="{{ path('mh_store_delete', {'id': product.id}) }}" method="post">

                        <strong>Voulez-vous vraiment supprimer ce produit ? &nbsp;</strong> <input type="submit" value="Supprimer" class="btn btn-danger" />

                        {{ form_rest(form) }}
                    </form>
                </div>
            </div>
        </div>
    </div>




{% endblock %}